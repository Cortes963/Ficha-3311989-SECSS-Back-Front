$kind: http-request
name: 05 Filtrar veh?culos dentro
url: "{{baseUrl}}/api/input_output?estado=DENTRO&pagina=1&limite=20"
method: GET
headers:
  Authorization: Bearer {{tokenCelador}}
queryParams:
  estado: DENTRO
  pagina: "1"
  limite: "20"
scripts:
  - type: afterResponse
    code: pm.test("HTTP 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 5000
