/**
 * Backend module: controller/attention.controller.js
 * HTTP handlers and infrastructure for this resource.
 * Validates input, executes database work, and returns API responses.
 */

import db from '../db.js';
import { actorId, error, integer, page, required, PQRS } from '../lib.js';

// GET /reportes: list reports with pagination.
export async function indexReport(req,res){try{const {pagina,limite,offset}=page(req.query);const [[c]]=await db.query('SELECT COUNT(*) total FROM reporte');const [datos]=await db.query(`SELECT r.*,CONCAT(u.primer_nombre,' ',u.primer_apellido) celador FROM reporte r JOIN usuario u ON u.id=r.id_usuario_celador ORDER BY r.fecha_hora DESC LIMIT ? OFFSET ?`,[limite,offset]);res.json({ok:true,pagina,limite,total:c.total,datos});}catch(e){return error(res,e)}}
export async function storeReport(req,res){try{required(req.body,['asunto','cuerpo']);const [r]=await db.query('INSERT INTO reporte (id_usuario_celador,fecha_hora,asunto,cuerpo,estado,id_entrada_salida) VALUES (?,NOW(),?,?,0,?)',[actorId(req),req.body.asunto,req.body.cuerpo,req.body.id_entrada_salida||null]);res.status(201).json({ok:true,id_reporte:r.insertId});}catch(e){return error(res,e)}}
export async function showReportId(req,res){try{const id=integer(req.params.id,'id');const [rows]=await db.query('SELECT * FROM reporte WHERE id=?',[id]);if(!rows.length)return res.status(404).json({ok:false,mensaje:'Reporte no encontrado.'});if(req.user.roles.includes('JEFE_SEGURIDAD')&&!rows[0].estado)await db.query('UPDATE reporte SET estado=1 WHERE id=?',[id]);res.json({ok:true,datos:{...rows[0],estado:req.user.roles.includes('JEFE_SEGURIDAD')?1:rows[0].estado}});}catch(e){return error(res,e)}}
export async function indexPqrs(req,res){try{const {pagina,limite,offset}=page(req.query);const own=req.user.roles.includes('ADMINISTRADOR')?'':'WHERE p.id_usuario=?';const args=own?[]:[actorId(req)];const [[c]]=await db.query(`SELECT COUNT(*) total FROM pqrs p ${own}`,args);const [datos]=await db.query(`SELECT p.*,EXISTS(SELECT 1 FROM respuesta r WHERE r.id_pqrs=p.id) tiene_respuesta FROM pqrs p ${own} ORDER BY p.fecha_creacion DESC LIMIT ? OFFSET ?`,[...args,limite,offset]);res.json({ok:true,pagina,limite,total:c.total,datos});}catch(e){return error(res,e)}}
export async function storePqrs(req,res){try{required(req.body,['asunto','cuerpo']);const [r]=await db.query('INSERT INTO pqrs (id_usuario,asunto,cuerpo,estado) VALUES (?,?,?,?)',[actorId(req),req.body.asunto,req.body.cuerpo,PQRS.RADICADO]);res.status(201).json({ok:true,id_pqrs:r.insertId});}catch(e){return error(res,e)}}
export async function storePqrsAnswer(req,res){const c=await db.getConnection();try{required(req.body,['asunto','cuerpo']);const id=integer(req.params.id,'id');await c.beginTransaction();const [p]=await c.query('SELECT id FROM pqrs WHERE id=? FOR UPDATE',[id]);if(!p.length)throw Object.assign(new Error('PQRS no encontrada.'),{status:404});const [exists]=await c.query('SELECT id FROM respuesta WHERE id_pqrs=?',[id]);if(exists.length)throw Object.assign(new Error('La PQRS ya fue respondida.'),{status:409});await c.query('INSERT INTO respuesta (id_pqrs,id_usuario_administrador,asunto,cuerpo) VALUES (?,?,?,?)',[id,actorId(req),req.body.asunto,req.body.cuerpo]);await c.query('UPDATE pqrs SET estado=? WHERE id=?',[PQRS.RESUELTO,id]);await c.commit();res.status(201).json({ok:true,mensaje:'Respuesta registrada.'});}catch(e){await c.rollback();return error(res,e)}finally{c.release()}}
export async function updatePqrsState(req,res){try{const id=integer(req.params.id,'id'),estado=Number(req.body.estado);if(!Object.values(PQRS).includes(estado))throw Object.assign(new Error('Estado PQRS inválido.'),{status:400});const [r]=await db.query('UPDATE pqrs SET estado=? WHERE id=?',[estado,id]);if(!r.affectedRows)return res.status(404).json({ok:false,mensaje:'PQRS no encontrada.'});res.json({ok:true,mensaje:'Estado actualizado.'});}catch(e){return error(res,e)}}
export async function indexAnswer(req,res){try{const [datos]=await db.query('SELECT * FROM respuesta ORDER BY id DESC');res.json({ok:true,datos});}catch(e){return error(res,e)}}

