$kind: http-request
name: 04 Consultar veh?culo propio
url: "{{baseUrl}}/api/vehicle/{{nuevoVehiculoId}}"
method: GET
headers:
  Authorization: Bearer {{tokenAprendiz}}
scripts:
  - type: afterResponse
    code: pm.test("HTTP 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 4000
