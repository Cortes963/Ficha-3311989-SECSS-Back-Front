$kind: http-request
name: GET quota/detalle/{{idAprendizNuevo}}/{{nuevoVehiculoId}} (admin)
url: "{{baseUrl}}/api/mobile/v1/quota/detalle/{{idAprendizNuevo}}/{{nuevoVehiculoId}}"
method: GET
headers:
  Authorization: Bearer {{tokenAdmin}}
scripts:
  - type: afterResponse
    code: pm.test("200 OK", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 6000
