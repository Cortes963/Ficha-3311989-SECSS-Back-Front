$kind: http-request
url: "{{baseUrl}}/api/pqrs?pagina=1&limite=20"
method: GET
headers:
  Authorization: Bearer {{tokenAprendiz}}
queryParams:
  pagina: "1"
  limite: "20"
scripts:
  - type: afterResponse
    code: pm.test("HTTP 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 2000
