$kind: http-request
url: "{{baseUrl}}/api/mobile/v1/users"
method: GET
scripts:
  - type: afterResponse
    code: pm.test("401 sin token", () => pm.response.to.have.status(401));
    language: text/javascript
order: 3000
