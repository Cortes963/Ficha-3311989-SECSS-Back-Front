$kind: http-request
url: "{{baseUrl}}/api/core/health"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("200 OK", () => pm.response.to.have.status(200));
      pm.test("ok:true", () => pm.expect(pm.response.json().ok).to.eql(true));
    language: text/javascript
order: 1000
