$kind: http-request
url: "{{baseUrl}}/api/mobile/v1/quota?pagina=1&limite=20"
method: GET
headers:
  Authorization: Bearer {{tokenAdmin}}
queryParams:
  pagina: "1"
  limite: "20"
scripts:
  - type: afterResponse
    code: pm.test("200 OK", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 1000
