$kind: http-request
url: "{{baseUrl}}/api/quota"
method: GET
headers:
  Authorization: Bearer {{tokenAprendizNuevo}}
scripts:
  - type: afterResponse
    code: pm.test("403 sin permiso", () => pm.response.to.have.status(403));
    language: text/javascript
order: 2000
