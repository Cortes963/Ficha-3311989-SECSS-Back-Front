$kind: http-request
name: 02 Centros p?blicos sin token
url: "{{baseUrl}}/api/core/centros-publicos"
method: GET
scripts:
  - type: afterResponse
    code: pm.test("HTTP 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 2000
