$kind: http-request
url: "{{baseUrl}}/api/users?rol=INVITADO"
method: GET
headers:
  Authorization: Bearer {{tokenCelador}}
queryParams:
  rol: INVITADO
scripts:
  - type: afterResponse
    code: pm.test("HTTP 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 6000
