$kind: http-request
url: "{{baseUrl}}/api/mobile/v1/reportes"
method: GET
headers:
  Authorization: Bearer {{tokenJefe}}
scripts:
  - type: afterResponse
    code: pm.test("200 OK", () => pm.response.to.have.status(200));
    language: text/javascript
order: 1000
