$kind: http-request
name: GET users?rol=INVITADO (celador, permitido)
url: "{{baseUrl}}/api/mobile/v1/users?rol=INVITADO"
method: GET
headers:
  Authorization: Bearer {{tokenCelador}}
queryParams:
  rol: INVITADO
scripts:
  - type: afterResponse
    code: pm.test("200 OK", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 11000
