$kind: http-request
url: "{{baseUrl}}/api/respuestas"
method: GET
headers:
  Authorization: Bearer {{tokenCelador}}
scripts:
  - type: afterResponse
    code: pm.test("403 sin permiso", () => pm.response.to.have.status(403));
    language: text/javascript
order: 13000
