$kind: http-request
url: "{{baseUrl}}/api/reportes/{{nuevoReporteId}}"
method: GET
headers:
  Authorization: Bearer {{tokenJefe}}
scripts:
  - type: afterResponse
    code: pm.test("200 OK", () => pm.response.to.have.status(200));
    language: text/javascript
order: 4000
