$kind: http-request
url: "{{baseUrl}}/api/mobile/v1/reportes/999999"
method: GET
headers:
  Authorization: Bearer {{tokenJefe}}
scripts:
  - type: afterResponse
    code: pm.test("404 no encontrado", () => pm.response.to.have.status(404));
    language: text/javascript
order: 5000
