$kind: http-request
name: GET users/elegibles (jefe busca candidatos a CELADOR)
url: "{{baseUrl}}/api/mobile/v1/users/elegibles"
method: GET
headers:
  Authorization: Bearer {{tokenJefe}}
scripts:
  - type: afterResponse
    code: pm.test("200 OK", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 9000
