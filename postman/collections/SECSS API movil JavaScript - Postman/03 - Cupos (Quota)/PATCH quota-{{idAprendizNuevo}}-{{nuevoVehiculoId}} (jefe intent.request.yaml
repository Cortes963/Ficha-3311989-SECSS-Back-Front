$kind: http-request
name: PATCH quota/{{idAprendizNuevo}}/{{nuevoVehiculoId}} (jefe intenta, esperado 403)
url: "{{baseUrl}}/api/mobile/v1/quota/{{idAprendizNuevo}}/{{nuevoVehiculoId}}"
method: PATCH
headers:
  Content-Type: application/json
  Authorization: Bearer {{tokenJefe}}
body:
  type: json
  content: |-
    {
      "estado": 0
    }
scripts:
  - type: afterResponse
    code: pm.test("403 esperado (solo ADMIN)", function () { pm.response.to.have.status(403); });
    language: text/javascript
order: 9000
