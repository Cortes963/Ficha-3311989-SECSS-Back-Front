$kind: http-request
name: GET users/{{nuevoAprendizId}} (admin)
url: "{{baseUrl}}/api/mobile/v1/users/{{nuevoAprendizId}}"
method: GET
headers:
  Authorization: Bearer {{tokenAdmin}}
scripts:
  - type: afterResponse
    code: pm.test("200 OK", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 13000
