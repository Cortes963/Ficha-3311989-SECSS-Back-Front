$kind: http-request
url: "{{baseUrl}}/api/quota/usuario/{{idAprendiz}}"
method: GET
headers:
  Authorization: Bearer {{tokenAprendiz}}
scripts:
  - type: afterResponse
    code: pm.test("HTTP 200", function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 2000
