# Copia este archivo a .env (no se sube al repo, ya está en .gitignore)
VITE_API_URL=http://localhost:4000/api

# Backend Java Spring Boot (modulo PQRS + Reporte), puerto por defecto 8080
VITE_PQRS_API_URL=http://localhost:8080/api
