$kind: http-request
url: "{{baseUrl}}/api/users/me"
method: GET
scripts:
  - type: afterResponse
    code: pm.test("HTTP 401 esperado", function () { pm.response.to.have.status(401); });
    language: text/javascript
order: 3000
