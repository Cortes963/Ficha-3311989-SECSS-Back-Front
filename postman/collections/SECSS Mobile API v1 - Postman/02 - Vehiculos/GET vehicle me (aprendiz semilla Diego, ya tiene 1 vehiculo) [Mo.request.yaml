$kind: http-request
name: GET vehicle me (aprendiz semilla Diego, ya tiene 1 vehiculo) [Mobile API]
url: "{{baseUrl}}/api/mobile/v1/vehicle/me"
method: GET
headers:
  Authorization: Bearer {{tokenAprendizSemilla}}
scripts:
  - type: afterResponse
    code: |-
      pm.test("200 OK", () => pm.response.to.have.status(200));
      if (pm.response.code === 200) {
        pm.test("al menos un vehiculo", () => pm.expect(pm.response.json().datos.length).to.be.above(0));
      }
    language: text/javascript
order: 1000
