$kind: http-request
name: PATCH centros/999999 (esperado 404)
url: "{{baseUrl}}/api/mobile/v1/core/centros/999999"
method: PATCH
headers:
  Content-Type: application/json
  Authorization: Bearer {{tokenAdmin}}
body:
  type: json
  content: |-
    {
      "nombre_centro": "No existe"
    }
scripts:
  - type: afterResponse
    code: pm.test("404 esperado", function () { pm.response.to.have.status(404); });
    language: text/javascript
order: 13000
