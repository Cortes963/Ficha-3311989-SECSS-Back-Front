$kind: http-request
url: "{{baseUrl}}/api/pqrs"
method: POST
headers:
  Authorization: Bearer {{tokenCelador}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "asunto": "x",
      "cuerpo": "y"
    }
scripts:
  - type: afterResponse
    code: pm.test("403 rol no permitido", () => pm.response.to.have.status(403));
    language: text/javascript
order: 9000
