$kind: http-request
name: PATCH vehicle/{{nuevoVehiculoId}}/inactivar (otra vez, esperado 404 ya inactivo)
url: "{{baseUrl}}/api/mobile/v1/vehicle/{{nuevoVehiculoId}}/inactivar"
method: PATCH
headers:
  Authorization: Bearer {{tokenAprendizNuevo}}
scripts:
  - type: afterResponse
    code: pm.test("404 esperado", function () { pm.response.to.have.status(404); });
    language: text/javascript
order: 9000
